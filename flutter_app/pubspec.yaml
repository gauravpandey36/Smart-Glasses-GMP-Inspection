name: gmp_inspector
description: GMP Floor Inspection Co-Pilot — Meta Ray-Ban + Claude AI
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  meta_wearables: ^0.1.0
  http: ^1.2.0
  flutter_tts: ^4.2.0
  image_picker: ^1.0.0
  path_provider: ^2.1.0
  share_plus: ^10.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
